<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Keep up/downloads alive while backgrounded (V2-563). A dataSync
         foreground service tells the OS not to suspend/kill the transfer; the
         wake lock keeps the CPU running with the screen off. POST_NOTIFICATIONS
         (API 33+) is required to show the service's ongoing notification. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Allows http:// EVM RPC for the local devnet flow. Mainnet RPCs use
         HTTPS, so this isn't needed in production. -->
    <application
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- WalletConnect spike: deep link the wallet uses to return to us
                 after signing. Must match the `redirect` in
                 WalletConnectManager.configure (antdemo-wc://request). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="antdemo-wc" android:host="request" />
            </intent-filter>
            <!-- autonomi:// content links (ant-webex scheme). Opens the app,
                 downloads the addressed content, and shows the Downloads tab. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="autonomi" />
            </intent-filter>
        </activity>
        <!-- Foreground service that keeps transfers running when backgrounded. -->
        <service
            android:name=".transfer.TransferService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- Shares saved datamap files with other apps (the "Open file" action). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
